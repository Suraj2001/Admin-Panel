import { FeatherIconsDirective } from './feather-icons.directive';

describe('FeatherIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatherIconsDirective();
    expect(directive).toBeTruthy();
  });
});
